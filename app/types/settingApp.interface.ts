import type { PrimaryColors } from "./primaryColors.interface";

export interface SettingApp {
    isMenuCollapsed : boolean,    
    isDark : boolean,    
    isMenuHorizontal : boolean,
    menuClass : string,
    topbarClass : string,
    fondoClass : string,
    primaryColor : string
}